{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "${workspaceFolder}/project/custom-build.sh",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}

